version: "3.9"
services:
  vulnerable-app:
    build: ./app
    container_name: lab09-vulnerable-app
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=development
      - DEBUG=true
      - DB_PASSWORD=SuperSecret123
    restart: unless-stopped

networks:
  default:
    name: lab09-net
